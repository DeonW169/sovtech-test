import React from 'react';
import { shallow } from 'enzyme';
import { NavbarComponent } from '../../components/Navbar';

// test('should correctly render Navbar', () => {
//   const wrapper = shallow(<NavbarComponent />);
//   expect(wrapper).toMatchSnapshot();
// });